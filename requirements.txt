# Local-runnable dependencies (M2 8GB friendly).
# For cloud-GPU deps (Boltz-2, OpenMM, PySB, PhysiCell), see requirements-cloud.txt

torch>=2.2,<3.0
transformers>=4.40,<5.0
biopython>=1.83
biotite>=0.41
numpy>=1.26,<3.0
pandas>=2.2
matplotlib>=3.8
